const person = {
    name: "Puff",
    age: 45,
    country:"Uganda"
  };
  console.log(`${person.name} is ${person.age} years old and born in ${person.country}`)

  console.log(`${person.name} the Magic Dragon lasts for 30 minutes `)

